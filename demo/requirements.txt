# Intentionally pinned to vulnerable versions for CVE agent testing.
# Each pin maps to a published CVE so the audit pipeline has real findings.

# CVE-2018-1000656 — open redirect / debug PIN issues; many transitive CVEs in this line
Flask==0.12.2

# CVE-2019-14806 — Werkzeug debugger PIN bypass (also tied to Flask 0.12.x)
Werkzeug==0.14.1

# CVE-2019-10906 — Jinja2 sandbox escape via str.format_map
Jinja2==2.10

# CVE-2020-14343 — yaml.load() arbitrary object instantiation (RCE)
# CVE-2017-18342 — same family, default Loader unsafe
PyYAML==3.13

# CVE-2018-18074 — Authorization header leaked to third-party host on redirect
requests==2.19.0

# CVE-2019-11324 — CRLF injection via crafted URL
# CVE-2020-26137 — CRLF in HTTP request method
urllib3==1.24.1

# CVE-2021-43818 — lxml.html.clean allows XSS via crafted SVG/script
# CVE-2021-28957 — XML External Entity expansion in lxml
lxml==4.6.0

# CVE-2013-7459 — heap buffer overflow in ALGnew (RCE)
# pycrypto is abandoned — should be replaced with pycryptodome
pycrypto==2.6.1

# CVE-2020-5313 — Pillow buffer over-read in libFLI
# CVE-2019-16865 — DoS via crafted image
Pillow==5.3.0
